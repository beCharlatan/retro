# План модернизации: Bun bundler + ESM, Web Components (Lit), CSS Modules

Финализировано. Решения по развилкам (2026-09-12):

1. **Shadow DOM** (нативный Lit, `css\`\`` на компонент) — не light DOM.
2. **Тесты мигрируют на `data-testid`** — CSS Modules хэширует классы
   свободно, тестовые селекторы больше не зависят от имён классов.
3. **Инкрементальный выкат** с пилотной игрой (`dictator`) — остальные
   12 по группам с прогоном тестов после каждой.
4. Дополнительно: **Biome** (линт+форматирование) и **`bun test`** для
   чистой логики (`Roles.makePairs`/`makeGroups`/`swapIn*` и т.п.) в
   дополнение к Playwright. TypeScript — вне текущего объёма.

Важное следствие (см. «Ключевой факт» ниже): переход общих модулей на
`export`/`import` **не может** быть постепенным по одному файлу — в
классическом (не-ESM) скрипте `export` — синтаксическая ошибка, а раз
один файл становится модулем, все 12 остальных, ссылавшихся на его
глобальные имена, должны получить `import` в тот же момент. Поэтому
«Фаза 1» ниже — один атомарный коммит на все 18 файлов (`src/*.js` +
все игры), не по одному файлу. Инкрементальность (по пилоту, дальше по
играм) начинается только в Фазе 2 — где встаёт вопрос компонентной
модели (Lit/Shadow DOM/CSS Modules), а не модульной системы.

## Зачем этот документ

Текущая архитектура (см. README) — 13 игр + 5 общих паттернов
(`roles.js`, `screen.js`, `persist.js`, `print.js`, `chart-tip.js`) +
`app.js`, все как классические (не-ESM) скрипты, делящие один глобальный
scope, конкатенируемые `build.js` в единый `dist/index.html`. Это
осознанный выбор ради простоты и нулевых зависимостей — но не
использует ни бандлер, ни компонентную модель, ни изоляцию стилей.

Цель модернизации — трижды заявленная (bundler+ESM, Web Components/Lit,
CSS Modules) плюс общее «современные практики» — при этом **не потеряв**:
- единый самодостаточный `dist/index.html` (двойной клик, без сервера);
- всё поведение 404 Playwright-тестов (см. `test/README.md`);
- `sessionStorage`-черновики, приватность общего экрана (блюр полей),
  PDF-экспорт, трио-фолбэк при нечётном числе участников и т.д.

## Текущая архитектура (снимок на момент анализа)

| Файл/группа | Строк | Суть |
|---|---|---|
| `src/games/*.js` (13 файлов) | 234–414 каждый, ~4000 всего | Функция `renderXGame()`: один большой `innerHTML` шаблон + императивные `document.getElementById`/`querySelector` + `window.xGoTo = ...` для инлайновых `onclick` |
| `src/roles.js` | 270 | Чистые функции (`makePairs`/`makeGroups`, шаффл, свап) + HTML-хелперы + делегированные обработчики свапа |
| `src/screen.js` | 78 | `Screen.goTo/wireBackHome/updateProgress` — общая навигация по `.screen`/`.dot` |
| `src/persist.js` | 125 | `Persist.save/load/clear/offerRestore` — черновики в `sessionStorage`, баннер восстановления |
| `src/print.js` | 127 | `Print.mount/run` — печатная шапка/футер, `window.print()` |
| `src/chart-tip.js` | 85 | Подсказка при наведении на точку SVG-графика |
| `src/app.js` | 353 | `state`, `CATEGORY`, `GAMES`, главный экран, роутер `GAME_RENDERERS`, делегированный обработчик приватности toggle-кнопок |
| `src/styles.css` | 1227 | Один файл: токены (`:root`), типографика, все игровые компоненты, `@media print` |
| `test/*.spec.js` | 404 проверки | Playwright, реальный headless-браузер поверх **собранного** `dist/index.html`, селекторы по id/классам/`data-*`/тексту |

Ключевой факт (проверено экспериментально): всё это работает только
потому, что файлы делят один top-level scope (либо через конкатенацию в
`build.js`, либо через несколько классических, не-`type=module`,
`<script src>` в dev `src/index.html`). Настоящий ES-модуль (`import
"./file.js"`) **не** протаскивает `const X = ...` между файлами — Bun
(и любой ESM-бандлер) обернёт каждый файл в свой scope. Значит переход
на реальный `import`/`export` — это не косметика, а необходимость
явно проэкспортировать `Screen`, `Persist`, `Roles`, `Print`,
`ChartTip`, `state`, `avatarName`, `CATEGORY`, `showToast` и т.д. и
явно проимпортировать их в каждом из 13 файлов игр.

## Целевая архитектура (предлагается)

- **ESM everywhere.** Каждый файл — модуль с явными `export`/`import`.
  Игры оформляются как кастомные элементы (`<retro-game-anchoring>` и
  т.п.), общие паттерны — как небольшие ES-модули и/или Lit reactive
  controllers (переиспользуемая логика без собственного DOM):
  `RolesController`, `PersistController`, `ScreenController`.
- **Web Components на Lit** вместо строковых `innerHTML`-шаблонов:
  декларативные `render()` с Lit `html\`\`` и реактивными
  `@property`/`@state`, `@click=${...}` вместо инлайновых `onclick="..."`
  строк в разметке.
- **CSS Modules** (или Lit `css\`\`` — см. развилку №1) для новых/
  переносимых стилей вместо роста одного плоского `styles.css`.
- **Bun как бандлер**: `build.js` вызывает `Bun.build()` на реальном
  графе зависимостей (единая точка входа `src/main.ts`/`main.js`,
  импортирующая `<retro-app>`), результат — один JS-бандл + один
  CSS-бандл, которые build.js по-прежнему инлайнит в единый
  `dist/index.html` (эта часть шаблона в целом остаётся).
- **Lit** тянется как обычная `devDependency`/`dependency` через
  `bun install` и попадает внутрь бандла (никакого CDN/рантайм-запроса
  у пользователя — тот же принцип «один файл, без сети»).
- Поведение (черновики, приватность, PDF, трио) **не меняется** —
  меняется только то, *как* оно реализовано технически.

## Последствия принятых решений

### Shadow DOM (не light DOM)
Значит: (а) `styles.css` придётся разложить на общие design-token'ы
(CSS custom properties на `:root` документа — они пересекают границу
shadow root и продолжат работать) + собственные `css\`\`` блоки в
каждом компоненте для всего остального (типографика, `.wrap`,
специфичные для игры стили); (б) печать/PDF (`window.print()`,
`@media print`) нужно будет явно спроектировать заново — либо через
`::part()`/slotted-контент, либо печатный узел рендерится в light DOM
через `<slot>`, чтобы `@media print` в документ-уровневом стиле мог его
видеть — это прорабатывается на пилоте (Фаза 2) до переноса остальных
12 игр; (в) весь `document.getElementById(...)` в общих модулях и играх
переписывается на `this.renderRoot.querySelector(...)` внутри классов
Lit-компонентов.

### Тесты на `data-testid`
Все ~404 проверки, которые сейчас цепляются за CSS-классы/id
(`.entry-row`, `#entry-body`, `.draft-restore`, `button[data-val="C"]`
и т.п.), получают устойчивые `data-testid="..."` атрибуты — независимые
от того, как хэширует классы CSS Modules. Именно поэтому Playwright
должен уметь пронизывать Shadow DOM: он это делает автоматически для
CSS-селекторов (в т.ч. `[data-testid=...]`) на открытых (`open`) shadow
root — все Lit-компоненты создаются с `shadowRootOptions: { mode:
'open' }` (значение по умолчанию в Lit), это не отдельная задача.
Правка теста и правка компонента идут в одном коммите на пилоте —
дальше это шаблон для остальных 12 игр.

### Инкрементальный выкат, пилот — `dictator`
Общие модули переходят на ESM атомарно (Фаза 1, все 18 файлов сразу —
см. «Ключевой факт» выше), а вот перевод конкретной игры в Lit/Shadow
DOM/CSS Modules/data-testid идёт по одной игре за раз, начиная с
`dictator.js` (средняя сложность: два раунда, график, печать, черновик,
без ролей/пар — при этом достаточно богат, чтобы вскрыть все подводные
камни печати и черновиков до переноса остальных 12).

### Biome + `bun test`
Biome — линт и форматирование одним инструментом, конфиг
`biome.json`, скрипты `bun run lint` / `bun run format`. Уже применён
ко всему репозиторию (безопасные автофиксы + `node:`-протокол для
`require`); `biome.json`'s `overrides` для `src/*.js`/`src/games/*.js`
временно отключает `noUnusedVariables`/`noUnusedFunctionParameters` —
Biome анализирует файлы по одному и (справедливо, раз экспортов ещё
нет) считает неиспользуемыми `Screen`/`Persist`/`Roles`/`Print`/
`ChartTip`/`render*Game`, на самом деле потребляемые другими файлами
через общий scope. Убрать это исключение сразу после Фазы 1. `bun test`
покрывает чистую логику без DOM: `Roles.shuffle/makePairs/makeGroups/
swapInPairs/swapInGroups` в первую очередь (лежат в `test/unit/*.test.js`,
`bunfig.toml` указывает `[test] root` именно туда, чтобы не
пересекаться с Playwright-наборами в `test/*.spec.js`).

## Фазы

- ✅ **Фаза 0 — Подготовка.** `git init` + бейзлайн-коммит текущего
  зелёного состояния (404/404). `bun add lit`, `bun add -d
  @biomejs/biome`. `test/unit/` + `bunfig.toml` под `bun test`.
- ✅ **Фаза 1 — Общие модули → ESM (атомарно, все 18+ файлов).**
  Сделано иначе, чем в первом черновике плана: `app.js` (353 строки —
  state, аватары, главный экран, роутер) не осталось одним файлом с
  добавленным `export` — распалось на `state.js` (state/CATEGORY/
  STRUCTURE/GAMES/avatar-хелперы/`app`-узел), `toast.js`
  (`showToast`/`copyToClipboard`), `home.js` (главный экран, было
  внутри app.js), `router.js` (`GAME_RENDERERS`/`openGame`, импортирует
  все 13 игр) и `privacy-mask.js` (делегированный блюр toggle-кнопок,
  side-effect-only модуль). `app.js` теперь — только `import
  './privacy-mask.js'; import { renderHome } from './home.js';
  renderHome();` — точка входа для `Bun.build()`. `roles.js`,
  `screen.js`, `persist.js`, `print.js`, `chart-tip.js` и все 13 игр
  получили явные `export`/`import` (games/*.js импортируют `state`,
  `avatarName`, `app` из `state.js`, плюс `Screen`/`Persist`/`Print` и,
  где нужно, `Roles`/`ChartTip`/`copyToClipboard`). `build.js`
  переписан на `Bun.build()` (JS-граф от `src/app.js` + CSS от
  `src/styles.css`, оба с `minify:true`), инлайнится в
  `dist/index.html` как раньше — один `<script type="module">` вместо
  раньше нескольких склеенных вручную кусков.
  **Найденный и исправленный по ходу баг:** каждая игра вызывает PDF
  через инлайновый `onclick="Print.run()"` в HTML-разметке — а
  инлайновые обработчики выполняются в ГЛОБАЛЬНОЙ области видимости,
  не видят module-scoped `import { Print }`. Пофиксено добавлением
  `window.Print = Print;` в `print.js` (тот же паттерн, которым каждая
  игра уже давно вешает свои `window.xGoTo`/`xShowResults` — просто
  этот случай раньше был неявным глобалом, а не игровой функцией).
  Полный прогон `bun run test` — 404/404 зелёные, включая PDF-тесты,
  которые как раз это ловят.
  **Ещё находка:** `src/index.html` (dev-версия) грузила файлы через
  отдельные `<script src>` — с настоящими ES-модулями это перестаёт
  открываться напрямую по `file://` (браузеры блокируют `import` как
  cross-origin). Добавлен `scripts/dev-server.js` (`Bun.serve()`,
  ~20 строк) + `bun run dev` → `http://localhost:5173`; `dist/index.html`
  это не касается (там всё заинлайнено текстом, без отдельных запросов).
  Проверено вручную в браузере (Browser pane): главный экран и
  `anchoring` открываются и работают без ошибок в консоли.
  Biome: `overrides`, отключавший `noUnusedVariables` для `src/*.js`/
  `src/games/*.js` (см. ниже), убран — с реальными `export` ложных
  срабатываний не осталось; заодно всплыли и почищены 3 мелких
  реальных (неиспользуемый параметр `q` в availability.js/
  calibration.js, неиспользуемая `MT` в crowd-wisdom.js).
- ✅ **Фаза 2 — Пилотная игра (`dictator`).** Переписана как
  `<retro-game-dictator>` (`src/games/dictator.js`) на Lit, Shadow DOM
  (по умолчанию, `mode: 'open'`), общий `sharedStyles`
  (`src/styles/shared-styles.js` — **весь** `styles.css` целиком,
  импортированный как текст через `import ... with { type: 'text' }`
  + `unsafeCSS()`, а не вручную вырезанное подмножество правил — см.
  обоснование в комментарии файла: риск незаметно упустить, например,
  `@media print` или CSS-приватность полей, важнее лишней
  специфичности). Проверено экспериментально (`Bun.build` с
  `type: 'text'`), что это работает и под бандлером, и под
  `bun run dev`.

  **Архитектурные решения, зафиксированные здесь как образец для
  Фазы 3:**
  - Экраны не переключаются императивно (`Screen.goTo` не
    используется этим компонентом) — рендерятся все сразу, класс
    `.active`/`.done` на каждой `<section>`/точке прогресса вычисляется
    от реактивного `screenIdx` прямо в `render()`. Прогресс-бар и
    disabled-состояние кнопок — не `Screen.updateProgress`, а
    вычисляемые геттеры в шаблоне.
  - Баннер восстановления черновика рендерится декларативно из
    `this.draft`, а не через `Persist.banner()` (тот пишет
    `document.getElementById(mountId).innerHTML = ...`, не видит
    shadow root); для этого `timeAgo` вынесен из `persist.js` как
    самостоятельный `export`. `Persist.save/load/clear` — чистая
    работа с `sessionStorage`, не изменились.
  - `Print.mount(id, data, root = document)` — добавлен необязательный
    `root`, компонент передаёт `this.renderRoot`. `ShadowRoot`
    поддерживает `getElementById` нативно (часть
    `DocumentOrShadowRoot`), так что `root.getElementById(...)` работает
    одинаково что для `document`, что для shadow root.
  - Кнопка PDF — настоящий `@click=${() => Print.run()}` вместо
    инлайнового `onclick="Print.run()"` (поэтому `window.Print`-мост из
    Фазы 1 этому компоненту не нужен — нужен только оставшимся 12
    играм).
  - SVG-график по-прежнему строится императивно (`ChartTip`'s
    hover-логика не ложится на декларативный рендер естественно) —
    просто через `this.renderRoot.getElementById(...)` вместо
    `document.getElementById(...)`.
  - `Roles.dotsHTML()` для этого компонента не используется (он не
    проставляет `.done`, только `.active` — терялась бы анимация
    прогресса); точки рисуются прямо в шаблоне маленьким циклом.

  **Тесты:** `test/games.js`'s дескриптор dictator и
  `test/chart-tip.spec.js`'s dictator-специфичный блок переведены на
  `data-testid="entry-body-1"`/`"entry-body-2"`/`"next-btn-1"`/
  `"next-btn-2"` вместо `id`; `#pdf-btn`/`#dict-chart`/
  `#print-header-dictator`/`#print-footer-dictator` **оставлены как
  `id`** — это общие межигровые селекторы в `pdf.spec.js`/
  `chart-tip.spec.js`, которые проверяют все 13 игр одним и тем же
  кодом, не специфика data-testid-миграции.
  Ещё один реальный, некосметический фикс, который потребовался в
  тестовой инфраструктуре ДО переноса dictator: `persistence.spec.js`
  и `pdf.spec.js` использовали `page.evaluate(() =>
  document.querySelector(...))` — это **не** пронизывает Shadow DOM (в
  отличие от `page.click`/`page.$`/`page.$eval`, которые используют
  CSS-движок Playwright и пронизывают открытые shadow root
  автоматически). Переведены на `page.$eval`/`page.$$eval` — исправлено
  и проверено ДО переноса dictator (на всех 13 ещё-legacy играх), чтобы
  не путать причины при отладке самого компонента.

  Полный прогон `bun run test` — 404/404. Проверено и вручную в
  браузере (Browser pane): т.к. инструменты панели (`find`/`read_page`/
  `get_page_text`) сами не пронизывают shadow root, использовались
  скриншоты + `javascript_tool` (`el.shadowRoot.querySelector(...)`)
  для управления — визуально: дизайн-система, аватары, блюр
  приватности, график с подсказками, таблица результатов — всё
  идентично прежнему виду.
- ✅ **Фаза 3 — Остальные 12 игр.** Все переведены по одной, каждая со
  своим прогоном `bun run test` (404/404) и коммитом — фактически ни
  одна не потребовала больше одной попытки:
  1. `public-goods` — клон dictator без графика.
  2. `anchoring` — scatter-график, корреляция, group-compare; первая
     игра, чей `#entry-body`/`#results-table` были захардкожены
     напрямую в `privacy.spec.js`/`chart-tip.spec.js` (не только через
     `test/games.js`) — переведены на `data-testid="entry-body"`.
  3. `crowd-wisdom` — первая игра-викторина со своим вопросом; здесь
     выяснилось (и задокументировано в файле), что `data-testid` был
     нужен только из-за хэширования классов CSS Modules в будущем — id
     в Shadow DOM и так изолированы по компонентам, так что дальше все
     игры просто сохраняют оригинальные id без миграции на
     data-testid. Заодно нашёлся и починен пробел: у dictator/
     public-goods/anchoring результатные таблицы потеряли
     `id="results-table"`/`id="results-tbody"` в Lit-переписывании —
     восстановлено во всех четырёх.
  4. `false-consensus` — первая игра с `.toggle-pair` (Да/Нет). Нашёлся
     реальный баг в **общем** модуле: `privacy-mask.js`'s
     делегированный `document`-слушатель использовал `e.target`,
     который для Shadow DOM ретаргетится на сам custom element (не на
     настоящую кнопку) — событие пересекает границу shadow root.
     Исправлено на `e.composedPath()[0]` — работает одинаково и для
     новых Shadow DOM игр, и для старых light-DOM (composedPath()[0]
     всегда указывает на реальный источник события).
  5. `barnum` — первая игра с копированием в буфер обмена;
     `copyToClipboard()` не потребовал изменений (работает с переданным
     элементом кнопки напрямую).
  6. `ultimatum` — первая игра с парами/click-to-swap. `Roles.pairsHTML()`/
     `bindPairSwap()`/`bindShuffle()` НЕ используются в Lit-версии —
     карточки пар и swap-состояние переписаны декларативно
     (`selectedSwapName` reactive state), для консистентности с
     остальными общими паттернами (хотя технически `bindNameSwap()`'s
     listener, привязанный к самому контейнеру, а не к `document`, и
     так сработал бы без ретаргетинга). `Roles.makePairs()`/
     `swapInPairs()` (чистые данные) переиспользуются как есть.
  7. `prisoners-dilemma` — тот же паттерн + `.toggle-pair` (Сотр./Пред.)
     на каждой стороне пары + экран-recap между раундами.
  8. `endowment` — первая игра с группами/click-to-swap (аналог
     ultimatum для `Roles.makeGroups()`/`swapInGroups()`).
  9. `framing` — группы + копирование в буфер + независимые
     спойлер-тексты (`textHidden = {a, b}` reactive state). Один
     осознанно сохранённый «странный» момент: кнопка «Вносить данные →»
     всегда пересоздаёт пустые entries для текущих групп — это
     поведение уже было в legacy-версии, не новое решение этой
     миграции.
  10. `availability` — 4 повторяющихся экрана-вопроса, сгенerированных
      из массива `QUESTIONS`, тот же `.toggle-pair`.
  11. `planning-fallacy` — простая solo-игра, два поля на человека, без
      графика.
  12. `calibration` — последняя игра: диапазон low/high на вопрос ×
      3 вопроса, каждый из которых можно заменить независимо (общий
      случай custom-question-панели crowd-wisdom/false-consensus).
      Подсказки/плейсхолдеры в панели читаются из `DEFAULT_QUESTIONS`,
      а не из текущих (возможно кастомных) `this.questions` — так же,
      как в legacy-версии, где эта панель строилась один раз при
      исходных значениях.

  Итог Фазы 3: **404/404 с первой попытки почти во всех 12 играх** —
  единственные найденные баги (results-table id, `privacy-mask.js`'s
  event retargeting) были общими проблемами инфраструктуры, пойманными
  и исправленными один раз, а не повторяющимися по каждой игре
  проблемами.
- **Фаза 4 — Финализация.** Домашний экран (`app.js` → `<retro-app>`/
  `<retro-home>`) и общие паттерны переводятся на Lit-контроллеры;
  `styles.css` — на общие токены + `css\`\`` по компонентам; удаление
  отживших паттернов (`window.xGoTo`, инлайновые `onclick` — из них
  остался только `Screen`/legacy-путь, если он ещё где-то используется
  после Фазы 3; фактически все 13 игр уже не используют `Screen`/
  `Roles.pairsHTML`/`bindPairSwap`/`bindGroupSwap`/`bindShuffle`,
  так что эти функции в `roles.js`/`screen.js` теперь мертвы и подлежат
  удалению в этой фазе — вместе с `window.Print`-мостом в `print.js`,
  который был нужен только для инлайновых `onclick="Print.run()"` в
  legacy-играх).
- **Фаза 5 — Документация и CI.** Обновление `README.md`/
  `test/README.md`, `bun run lint`/`bun run format` в CI.

## Риски и как их держим под контролем

- **Единственная сеть безопасности — 404 Playwright-теста.** Каждая
  фаза заканчивается зелёным прогоном `bun run test` (или релевантного
  поднабора) перед коммитом.
- **git обязателен** для этого объёма правок — без него нет
  возможности откатить фазу, если что-то пошло не так на середине.
- **PDF/печать — самая хрупкая часть** при выборе Shadow DOM (развилка
  №1) — стоит явно протестировать печать после первой же игры с новым
  стеком, а не откладывать на конец.
- **Инкрементальный выкат** (развилка №3а) — самый низкий риск: в
  любой момент есть последний зелёный чекпоинт, откуда можно
  продолжить или отступить.
